[package]
name = "snakebot-bot"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
snakebot-engine = { path = "../engine" }
rayon = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
serde_json = "1.0"
